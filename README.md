# 実解析学 ゼミ資料

ゼミ向けに作成する実解析学のレポート・講義ノートをまとめたリポジトリである。定義・定理・例題を体系的に整理し、学習者が追いやすい構成を目指す。

## 内容

- LaTeX の本文は `src/` 以下に配置する
- 図版は `src/chapters/img/` に格納する
- 生成物は `build/` に出力する（リポジトリには含めない）

## 章構成

章ファイルは `src/chapters/chNN.tex` 形式で管理し、`src/main.tex` から統合する。各章は「導入→定義→要点→手順→例題→まとめ」の流れを基本とする。
